import React from 'react'
import { MetricCardProps } from '../../../services/types'

// ======================================================
// COMPONENT: MetricCard (Safely Emptied)
// ======================================================

export const MetricCard: React.FC<MetricCardProps> = () => {
  // Returns nothing to the DOM, but keeps the app from breaking
  // if this component is still imported elsewhere.
  return null
}

export default MetricCard